config clang-win-base extends clang-base {

    # Specific attributes for the archive utility
    archive: {
        extension: ".lib",
        flags: [
            "-NOLOGO",
            "-OUT:${archive}"
        ]
    },

    # Specific attributes for the shared stage
    executable: {
        extension: ".exe",
        flags: [
            "-DEFAULTLIB:libcmt",
            "-NOLOGO",
            "-OUT:${executable}"
        ]
    }
}
