# The sbs-objects target compiles all the source files
compile sbs-objects {
    includes: [
        "../fllib/include"
    ],
    output_dir: "obj/${triplet}/${sbs.target}",
    sources: [
        "src/.*[.]c$"
    ]
}
